{
  "name": "scribal",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm:dev -w server\" \"npm:dev -w client\"",
    "dev:single": "npm:dev -w server",
    "install:all": "npm install -w server && npm install -w client",
    "build": "npm run build -w client && npm run build -w server",
    "start": "node server/dist/index.js",
    "serve": "npm run build -w client && npm run build -w server && node server/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
